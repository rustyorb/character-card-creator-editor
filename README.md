

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env` and set `VITE_GEMINI_API_KEY` to your Gemini API key:
   ```bash
   cp .env.example .env
   # Edit .env and set your key
   ```
3. Run the app:
   `npm run dev`
